//BYTE TANIMLÝ BÝR DÝZÝNÝN KUCUKTEN BUYUGE SÝRALÝ OLUP OLMADÝGÝNÝ BULUNUZ?
myds SEGMENT PARA 'veri'
dizi DB 1,2,3,4,5
N    DW 5
s    DB 0
myds ENDS

myss SEGMENT PARA 'yigin'
DW 20 DUP(?)
myss ENDS

mycs SEGMENT PARA 'kod'
ASSUME CS:mycs,SS:myss,DS:myds

MAIN PROC FAR

PUSH DS
XOR AX,AX
PUSH AX
MOV AX,MYDS
MOV DS,AX

	XOR SI,SI
	MOV CX,N
	MOV BX,0
	DEC CX
	DON:CMP BX,0
	JNE SON_IF
	CMP SI,CX
	JGE SON_IF
	MOV AL,DIZI[SI]
	CMP AL,DIZI[SI+1]
	JLE ARTTIR
	MOV BX,1
ARTTIR:INC SI
	JMP DON
SON_IF:CMP BX,0
	JNE SON
	MOV S,1
SON:RETF

	MAIN ENDP
